name: Hello world Worlflow

on: 
 push:
  branches: 
         - main 
 pull_request:
  branches: 
         - main
 workflow_dispatch:

jobs: 
  hello:
     runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout@v2 
       - name: hello world 
         run: echo "hello World" 
         shell: bash 

  goodbye:
      runs-on: ubuntu-latest 
      steps:
        - uses: actions/checkout@v2
        - name: Hello Parth Goodbye!!!!!!
          run: echo "Hello Parth"
          shell: bash 
           
   
  
 
